# Copy to .env.mobile-production only when preparing the Google Play build.
VITE_API_BASE_URL=https://console.gmed-health.com

# Production API CORS must allow the exact Android WebView origin:
# https://localhost

# CAPACITOR_APP_ID=com.gmedhealth.console
# CAPACITOR_APP_NAME=GMED Console
