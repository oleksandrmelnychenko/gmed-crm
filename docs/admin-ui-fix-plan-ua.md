# План виправлення UI адмін-сторінок (без змін таблиць)

Дата і час фіксації (Europe/Kyiv): **2026-04-22 15:37:18**

Підпис: **Codex (GPT-5)**

## Обмеження
- Таблиці не змінювати: без правок структури, колонок, рядків, cell-стилів і табличної поведінки.

## План по сторінках

### 1) Пользователи и роли (`admin-users`)
- Перевести shell сторінки на `PageHeader + AdminToolbar + AdminInlineMetric + AdminSheetScaffold`.
- Уніфікувати форми/контроли (`h-9`, `rounded-lg`, `inputClass/selectClass/textareaClass`).
- Уніфікувати `loading/error/success/empty` через `TabLoader` і `Banner`.
- Таблицю лишити без змін, вирівняти лише обгортки та стани навколо неї.

### 2) Матрица доступа (`admin-access`)
- Зберегти поточну архітектуру.
- Вирівняти вертикальний ритм header/toolbar/metrics/banner.
- Уніфікувати правий sheet під стандартні токени (`tokens.surface.softCard`, `Field`).
- Матрицю (таблицю) не змінювати.

### 3) Журнал активности (`admin-activity`)
- Уніфікувати нетабличні елементи toolbar (пошук/фільтр/reset) за розмірами і відступами.
- Привести detail sheet до єдиного патерну header/body/footer.
- Таблицю журналу не змінювати.

### 4) Объявления (`admin-announcements`)
- Перевести сторінку на сучасний адмін-shell (`PageHeader`, toolbar дій, `TabLoader`, `Banner`).
- Привести форму створення до уніфікованого стилю (sheet/dialog + стандартні контролі).
- Окремо оновити shell для `Новое объявление` (контейнер форми створення) без змін кнопки відкриття.
- Таблицю оголошень не змінювати.

### 5) DSGVO (`admin-compliance`)
- Зберегти функціональну структуру.
- Уніфікувати секції, форми, кнопки і стани за токенами `record-workspace`.
- Забрати локальні стилі, що дублюють системні токени (поза таблицями).
- Усі таблиці залишити без змін.

### 6) Пользовательские поля (`admin-custom-fields`)
- Міграція shell-частини на той самий патерн, що в нових адмін-модулях.
- Уніфікувати форму створення, повідомлення і стани завантаження/помилок.
- Таблицю custom fields не змінювати.

### 7) Состояние системы (`admin-health`)
- Точкова уніфікація нетабличних секцій: header, metrics, banner, cards.
- Вирівняти типографіку і spacing під базовий admin-pattern.
- Таблицю `database tables` не змінювати.

### 8) Уведомления (`admin-notifications`)
- Залишити як базовий референс.
- Дочистити успішні/помилкові флеші до єдиного banner-підходу.
- Таблицю каналів не змінювати.

### 9) Безопасность (`admin-security`)
- Уніфікувати обв’язку дій, банери та форми в sheet (`maintenance`, `ip add`).
- Вирівняти мікротипографіку і відступи секцій.
- Таблиці security залишити без змін.

### 10) Системные настройки (`admin-settings`)
- Зберегти як еталон для адмін-форм.
- Точкове вирівнювання дій/відступів/попереджень у sheet.
- Таблиці `pending MFA` та `active sessions` не змінювати.

## Порядок реалізації
1. `admin-users`
2. `admin-custom-fields`
3. `admin-announcements`
4. Точкова уніфікація: `admin-access`, `admin-activity`, `admin-health`, `admin-notifications`, `admin-security`, `admin-settings`, `admin-compliance`

## Поточний прогрес
- Виконано: `admin-users`, `admin-custom-fields`, `admin-announcements` (включно з shell `Новое объявление`).
- Виконано: уніфіковано success-повідомлення через `SuccessBanner` на `admin-notifications`, `admin-security`, `admin-settings`, `admin-compliance`.
- Виконано: shell-cleanup і текстові артефакти на `admin-access`, `admin-activity`, `admin-health`, `admin-compliance`.
- Виконано: фінальний технічний micro-sweep по 7 адмін-сторінках без змін таблиць.
- Залишилось: лише візуальний QA-прохід у браузері (desktop/mobile) для остаточного підтвердження spacing/мікроконсистентності.

## Критерій готовності
- Однаковий shell UX на всіх 10 сторінках без змін у табличних блоках.
- Єдина поведінка loading/error/success/empty у нетабличних частинах.
- Єдині токени контролів, відступів, радіусів і заголовків.
